# Build riêng cho app mobile (Capacitor). WebView của Capacitor tự chạy ở
# hostname "localhost" nên logic tự dò API trong apiClient.js sẽ nhầm là đang
# dev và gọi vào localhost:4000 nếu không có biến này ép về production.
VITE_API_BASE_URL=https://peaceflow-app.vercel.app/api/v1
